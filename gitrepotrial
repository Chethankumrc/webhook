pipeline {
    agent any
    
    environment{
    PATH = "/opt/apache-maven/bin:$PATH"

    stages {
        stage('git checkout) {
            steps {
                echo 'Hello World'
            }
        }
    stages {
        stage('Maven build') {
            steps {
                sh "mvn clean package"
            }
        }
    
    }
}
